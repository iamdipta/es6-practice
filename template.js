const firstName = "Dipta"
const lastName = "Roy"
const fullName = firstName + " " + lastName + " is a good boy";

// here is the simple es6 string template feature what should allow u to easier 
const fullName = `${firstName} ${lastName} is a good Boy`;
// u can also use calculation value as string value
const fullName = `${firstName} ${106 + 20} is a good Boy`;

const nam = "Adam Sandler";
const greet = `hello ${nam},welcome to the club`;
console.log(greet)